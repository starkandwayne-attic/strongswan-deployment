---
properties:
  strongswan:
    local_subnet:  (( param "What networks (in comma-separated CIDR notation) should strongswan advertise to the remote side of the tunnel?" ))
    remote_subnet: (( param "What networks (in comma-separated CIDR notation) should strongswan advertise to the remote side of the tunnel?" ))
    tunnels: (( param "Enter the ip/subnet/PSK of each tunnel to configure in an array of maps" ))
